https://docs.google.com/document/d/1jw2jLgGun4HV8LOziOAbAZQkAGqUn54X89EwfUdkZFw/edit?tab=t.7atnuuko46x#heading=h.9y0ihcb6g9ge
рактическая работа выполняется на ПЭВМ. Каждая сеть эмулируется одной ОС (Виртульной машиной) из заданной подсети.

Необходимо на каждой машине настроить сеть и по необходимости - брандмауэр (сетевой экран) Iptables.

Рабочая настроенная сеть сдается лично преподавателю. В электронном виде (в ответе)  сдаются команды, выполняемые на всех виртуальных машинах.

пример отчета:

 host1 (net1) 32 хоста максимально:
  адрес сети:  
  маска сети:
   команды:
    .....
   сохраненное состояние iptables (iptables-save)
 host2 (net2) 320 хостов максимально:
  адрес сети:  
  маска сети: 
   команды: 
    .....
   сохраненное состояние iptables (iptables-save)
 host3 (server):
  адрес сети:
  маска сети:  
   команды:
    .......
   сохраненное состояние iptables (iptables-save)
//////////Задание
на рисунке у меня нарисована сеть 
-слева 1 сеть на 32 хоста,  на нем или на одном только пк(не понятно) запрет на подключение к серверу  1 сети кроме ssh, запрет icmp
-справа 2 сеть 16 хостов, на верхнем пк(одном из них) запрет на подключение к серверу со 2 сети кроме ftp,http,https
на нижнем разрешить интернет, пропускать только http и https, im клиенты
-посередине по картинке похож на блок пк, слева снизу справа у него кирпичные стены, а внизу после кирпича идет соединение к интернету
