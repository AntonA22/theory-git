# theory-git
Вот краткая теория по основным командам Git — удобно для шпаргалки 👇

⸻

🧩 1. Настройка

git config --global user.name "Имя"
git config --global user.email "email@example.com"

Задает имя и почту, которые будут отображаться в коммитах.

⸻

📦 2. Создание и инициализация репозитория

git init

Создает новый локальный репозиторий в текущей папке.

git clone <url>

Клонирует существующий удалённый репозиторий (например, с GitHub).

⸻

💾 3. Основной цикл работы
	1.	Проверить статус:

git status

Показывает изменённые файлы и их состояние.

	2.	Добавить файлы в индекс (staging):

git add <имя_файла>
git add .   # добавить все изменения


	3.	Создать коммит:

git commit -m "Описание изменений"



⸻

🌿 4. Работа с ветками

git branch                # список веток
git branch <имя>          # создать новую ветку
git checkout <имя>        # перейти на ветку
git switch <имя>          # современная альтернатива checkout
git merge <ветка>         # слить ветку с текущей
git branch -d <имя>       # удалить ветку


⸻

🚀 5. Работа с удалённым репозиторием

git remote add origin <url>  # привязать удалённый репозиторий
git push -u origin main      # отправить коммиты на сервер
git pull                     # получить и объединить изменения
git fetch                    # просто загрузить изменения без объединения


⸻

🔄 6. История и откаты

git log               # история коммитов
git diff              # различия между версиями
git checkout <файл>   # вернуть файл к последней версии
git reset --hard <commit>  # откатить всё к указанному коммиту


⸻

⚙️ 7. Полезное

git stash             # временно сохранить незакоммиченные изменения
git stash pop         # вернуть их обратно
git rm <файл>         # удалить файл из проекта и индекса
git mv <старое> <новое>  # переименовать файл


⸻
